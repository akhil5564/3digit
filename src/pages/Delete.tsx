import React, { FC } from 'react';

interface DeleteProps {
  
}

const Delete: FC<DeleteProps> = ({}) => {
  return (
    <>
      <div>
        <input type="number" />
        <button></button>
      </div>
    </>
  );
};

export default Delete;